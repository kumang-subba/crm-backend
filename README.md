# CRM app

## Running locally

```shell
    git clone https://github.com/kumang-subba/crm-backend.git
    cd crm-backend
    npm i
    npm run start
```

environment variables:

```
MONGO_DB_URL=<your_mongo_db_url>
JWT_SECRET_KEY=<your_secret_key>
```
